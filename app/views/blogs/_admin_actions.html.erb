<% if logged_in?(:website_admin) %>
	<nav class="breadcrumb admin_breadcrumb">
	<%= link_to fa_icon('file-text'), status_control_blog_path(blog), style: blog_status_color(blog) %>
	<%= link_to fa_icon('pencil-square-o'), edit_blog_path(blog) %>
	<%= link_to fa_icon('trash'), blog, method: :delete, data: { confirm: 'Are you sure?' } %>
	</nav>
<% end %>