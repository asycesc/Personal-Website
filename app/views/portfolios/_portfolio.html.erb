<div class="card" data-id="<%= portfolio.id %>">
	<%= image_tag portfolio.thumb_image if !portfolio.thumb_image.nil?%>
	<p class="card-text">
		<span><%= link_to portfolio.title, portfolio_path(portfolio.id) %></span>
		<span><%= portfolio.subtitle %></span>
	</p>
</div>
